#include "apue.h"
int main(void)
{
  printf("Hellow world from process ID %ld\n", (long)getpid());
  printf("HEllow world from process ID %ld\n", (long)getpid());
  printf("hellow world from process ID %ld\n", (long)getpid());
  exit(0);
}

